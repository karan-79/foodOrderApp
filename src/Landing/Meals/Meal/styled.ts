import { Box } from "@mui/material";
import styled from "styled-components";

export const MealWrapper = styled(Box)`
  display: flex;
  border: 1px solid #d2d2d2;
  border-radius: 5px;
  padding-right: 4px;
  padding-left: 4px;
  background-color: #fff;
  box-shadow: -2px 3px 5px rgba(34, 34, 34, 0.05);
`;
